{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "ai-dashboard": {
      "type": "local",
      "command": ["pnpm", "--silent", "--filter", "@zync/server", "mcp"],
      "enabled": true
    }
  }
}
