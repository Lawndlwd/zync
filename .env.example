# Required: Encryption key for the secrets vault
# Generate with: openssl rand -hex 32
SECRET_KEY=

# Optional infrastructure overrides (defaults shown)
PORT=3001
NODE_ENV=development

# All other configuration is managed through the Settings UI:
# - Integrations (Jira, GitLab, Telegram, WhatsApp, Gmail)
# - Agent settings (LLM models, schedules, briefings)
# - Voice settings (Wakeword, Whisper)
# - All secrets are stored in the encrypted vault
